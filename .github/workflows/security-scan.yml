name: Security Scan

on: [push, pull_request]

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  sca-analysis:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tool: [trivy, dependabot-check]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Trivy Security Scan
        if: matrix.tool == 'trivy'
        continue-on-error: true
        run: |
          echo "🔍 Running Trivy security scan (SCA)..."
          mkdir -p reports
          
          # Устанавливаем Trivy
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install -y trivy
          
          # Сканируем зависимости проекта
          trivy fs --format json --output reports/trivy-report.json ./app || {
            echo "⚠️ Trivy scan completed with warnings"
          }
          
          trivy fs --format table --output reports/trivy-report.txt ./app || true
          
          echo "✅ Trivy scan completed!"

      - name: Check Dependabot Alerts
        if: matrix.tool == 'dependabot-check'
        run: |
          echo "📊 Checking Dependabot Security Alerts..."
          echo "View alerts at: https://github.com/${{ github.repository }}/security/dependabot"
          
          # Проверяем наличие Dependabot alerts через GitHub API
          gh api repos/${{ github.repository }}/dependabot/alerts --jq 'length' > /tmp/dependabot-count.txt || echo "0" > /tmp/dependabot-count.txt
          COUNT=$(cat /tmp/dependabot-count.txt)
          echo "Found $COUNT Dependabot alerts"
          
          if [ "$COUNT" -gt 0 ]; then
            echo "⚠️ Security alerts found - check GitHub Security tab"
            gh api repos/${{ github.repository }}/dependabot/alerts --jq '.[] | select(.state=="open") | "\(.dependency.package.name) - \(.security_advisory.summary)"' || true
          else
            echo "✅ No open Dependabot alerts"
          fi

      - name: Upload SCA Reports
        if: matrix.tool == 'trivy'
        uses: actions/upload-artifact@v4
        with:
          name: sca-reports-trivy
          path: reports/trivy-*
          retention-days: 30
          if-no-files-found: warn

  dependency-check:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache Dependency Check Database
        uses: actions/cache@v3
        with:
          path: ~/.dependency-check/data
          key: dependency-check-db-${{ runner.os }}-v7
          restore-keys: |
            dependency-check-db-${{ runner.os }}-

      - name: Configure Dependency Check (JSON Feeds Only)
        run: |
          echo "⚙️ Configuring Dependency Check to use JSON feeds only (NO API)"
          mkdir -p ~/.dependency-check
          echo "data.nvd.validForHours=0" > ~/.dependency-check/dependency-check.properties
          echo "✅ Configuration: JSON feeds only, no API"

      - name: Download Pre-populated Database (Optional)
        continue-on-error: true
        run: |
          # Если у вас есть готовая база данных в GitHub Releases или другом источнике
          # Раскомментируйте и укажите URL:
          # mkdir -p ~/.dependency-check/data
          # wget -q YOUR_DATABASE_URL -O /tmp/db.tar.gz
          # tar -xzf /tmp/db.tar.gz -C ~/.dependency-check/data
          echo "ℹ️ Pre-populated database download skipped (not configured)"
          echo "To use: upload database to GitHub Releases and uncomment download step"

      - name: Run Dependency Check Scan
        continue-on-error: true
        timeout-minutes: 90
        run: |
          echo "🔍 Starting Dependency Check scan..."
          echo "Using JSON feeds only (NO NVD API)"
          
          mkdir -p reports
          mkdir -p ~/.dependency-check/data
          
          # Проверяем наличие базы данных
          DB_FILES=$(find ~/.dependency-check/data -type f 2>/dev/null | wc -l)
          
          if [ "$DB_FILES" -gt 50 ]; then
            echo "✅ Using cached database ($DB_FILES files)"
            USE_NOUPDATE="--noupdate"
          else
            echo "⚠️ No cached database found ($DB_FILES files)"
            echo "Will attempt scan without database update (may have limited results)"
            echo "Note: Reports will be generated but may not contain vulnerability data"
            echo ""
            echo "To get full vulnerability data:"
            echo "  1. Update database locally: dependency-check.sh --updateonly"
            echo "  2. Archive: tar -czf db.tar.gz ~/.dependency-check/data"
            echo "  3. Upload to GitHub Releases"
            echo "  4. Uncomment download step in workflow"
            USE_NOUPDATE="--noupdate"
          fi
          
          # Запускаем сканирование
          # Dependency Check может работать даже с пустой базой - просто не найдет уязвимости
          docker run --rm \
            -v "$PWD:/src" \
            -v "$PWD/reports:/report" \
            -v "$HOME/.dependency-check/data:/data" \
            -v "$HOME/.dependency-check/dependency-check.properties:/usr/share/dependency-check/conf/dependency-check.properties:ro" \
            owasp/dependency-check:latest \
            --scan /src/app \
            --format HTML \
            --format JSON \
            --out /report \
            --project "DVWA" \
            --data /data \
            $USE_NOUPDATE \
            --enableExperimental \
            --failOnCVSS 11 || {
              echo "⚠️ Scan completed with warnings"
              echo "This is normal if database is empty - scan will still generate reports"
            }
          
          # Проверяем, создалась ли база данных после сканирования
          NEW_DB_FILES=$(find ~/.dependency-check/data -type f 2>/dev/null | wc -l)
          if [ "$NEW_DB_FILES" -gt 50 ]; then
            echo "✅ Database is now available ($NEW_DB_FILES files) - will be cached for next run"
          else
            echo "⚠️ Database still empty ($NEW_DB_FILES files)"
            echo "Reports generated but may not contain vulnerability data"
          fi
          
          echo "✅ Scan completed!"

      - name: Check Generated Reports
        run: |
          echo "📊 Checking generated reports..."
          if [ -f "reports/dependency-check-report.html" ]; then
            echo "✅ HTML report generated: $(ls -lh reports/dependency-check-report.html)"
          else
            echo "❌ HTML report not found"
          fi
          
          if [ -f "reports/dependency-check-report.json" ]; then
            echo "✅ JSON report generated: $(ls -lh reports/dependency-check-report.json)"
            python3 -c "import json, os; data = json.load(open('reports/dependency-check-report.json')) if os.path.exists('reports/dependency-check-report.json') else {}; deps = len(data.get('dependencies', [])); vulns = sum(len(d.get('vulnerabilities', [])) for d in data.get('dependencies', [])); print(f'Scanned Dependencies: {deps}'); print(f'Total Vulnerabilities Found: {vulns}')"
          else
            echo "❌ JSON report not found"
          fi

      - name: Upload Security Reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: reports/
          retention-days: 30
          if-no-files-found: warn

  security-checks:
    runs-on: ubuntu-latest
    needs: [sonarqube, dependency-check]
    steps:
      - name: Security Scan Summary
        run: |
          echo "========================================"
          echo "✅ All security scans completed successfully!"
          echo "📁 Available reports:"
          echo "   - SonarQube (SAST) results: Check SonarCloud dashboard"
          echo "   - Dependency Check (SCA) reports: Download 'security-reports' artifact"
          echo "========================================"
